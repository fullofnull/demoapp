# 빌드 불필요한 캐시 및 출력물
.gradle
build

# Git 및 IDE 관련
.git
.gitignore
*.iml
.idea

# 배포 관련 (쿠버네티스 등)
deploy/kubernetes/
deploy/.dockerignore

# ❗ gradlew 및 gradle wrapper 파일 제외하지 않음 (허용)
!gradlew
!gradle/wrapper/gradle-wrapper.jar
!gradle/wrapper/gradle-wrapper.properties

